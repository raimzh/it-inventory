{
  "name": "it-inventory",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "predev": "powershell -ExecutionPolicy Bypass -File ./scripts/kill-ports.ps1",
    "dev": "concurrently -k -n BACKEND,FRONTEND -c cyan,magenta \"npm --prefix backend run start:dev\" \"npm --prefix frontend run dev\"",
    "install:all": "npm --prefix backend install && npm --prefix frontend install"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
